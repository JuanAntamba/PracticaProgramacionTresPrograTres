<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PracticaProgramacionTresPrograTres.Views.ListaSuscripcionesPage"
             Title="Suscripciones Guardadas">
    <CollectionView ItemsSource="{Binding Suscripciones}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <Label Text="{Binding Servicio}" FontAttributes="Bold" />
                    <Label Text="{Binding CorreoAsociado}" />
                    <Label Text="{Binding CostoMensual}" />
                    <Label Text="{Binding RenovacionAutomatica, StringFormat='Renovación: {0}'}" />

                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>